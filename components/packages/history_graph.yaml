# https://www.home-assistant.io/components/history_graph/

history_graph:
  fronius:
    name: Fronius Powerflow
    entities:
      - sensor.generation
      - sensor.consumption
      - sensor.voltage
    refresh: 60
